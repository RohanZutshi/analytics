x1=3 #assign
x2<-3 #same
x2
ls() #var in environment-listing
y=8
women
data() #shows the datasets available for use
?AirPassengers #shows details of data- data dictionary
library()
?mean #help
help(mean)
??mean #search through other sources
x <- c(0:10, 50)
x
xm <- mean(x)
c(xm, mean(x, trim = 0.10)) #trimmed 10% marks from both extreme ends and then mean is calculated
1:1000000
x<- c(0:10,50)
x
?c #combine
length(x)
xm
c
Sys.Date()
getwd()
plot(1:10) 
